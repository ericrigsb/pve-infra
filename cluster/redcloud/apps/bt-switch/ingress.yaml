apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: bt-switch-external
  namespace: bt-switch
  annotations:
    kubernetes.io/ingress.class: traefik
    external-dns.alpha.kubernetes.io/target: traefik.mesh.rigsb.net
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`bt-switch.mesh.rigsb.net`)
      kind: Rule
      services:
        - name: bt-switch-external
          port: 80
      middlewares:
        - name: limit-buffering
          namespace: traefik
        - name: proxy-headers
          namespace: traefik
        - name: security-headers
          namespace: traefik
  tls:
    secretName: bt-switch-mesh-rigsb-net-tls